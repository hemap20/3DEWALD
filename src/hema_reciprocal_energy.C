//calculation of reciprocal energy
#include "libinclude.h"
#include "const.h"
 //4 implementations for the energy calculation

 #define BASIC 1

 if defined BASIC
    double reci_energy(int K){
        for(int kx=-K, kx<K+1, kx++){
            for (int ky=-K, ky<K+1, ky++){
                for(int kz=-K, kz<K+1, kz++){
                    

                }
            }
        }
    }